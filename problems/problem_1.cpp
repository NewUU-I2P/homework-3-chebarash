float problemSolution1(float consumed_water)
{
    float cost;

    /*
    float a = (consumed_water > 30) ? 30.0 : consumed_water;
    float b = (consumed_water > 50) ? 20 : ((a == 30) ? (consumed_water - 30) : 0);
    float c = (consumed_water > 60) ? 10 : ((b == 20) ? (consumed_water - 50) : 0);

    cost += 13
    cost += a * 0.4;
    cost += b * 0.12;
    cost += c * 1.4;
    cost += ((consumed_water - 60) > 0 ? (consumed_water - 60) : 0) * 1.5;

    */
    return cost;
}
